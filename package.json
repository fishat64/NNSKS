{
  "name": "gpg-keysrv-identity",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --driver-package meteortesting:mocha"
  },
  "dependencies": {
    "@babel/runtime": "^7.0.0",
    "gpg": "^0.6.0",
    "meteor-node-stubs": "^0.4.1",
    "openpgp": "^4.4.6"
  },
  "devDependencies": {
    "chai": "^4.1.2"
  }
}
